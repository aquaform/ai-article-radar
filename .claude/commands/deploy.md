Задеплой это приложение в интернет через Dokploy.\n\nШаги:\n1. Убедись, что тесты зелёные и сборка проходит. Если нет — остановись и доложи.\n2. Проверь, что есть Dockerfile и эндпоинт /health.\n3. Запусти деплой приложения с applicationId = $ARGUMENTS.\n   Предпочтительно: через MCP-инструмент Dokploy (deploy + опрос статуса).\n   Фолбэк (если MCP нет): POST на $DOKPLOY_URL/api/application.deploy\n   с заголовком x-api-key и телом {\"applicationId\": \"$ARGUMENTS\"}.\n4. Опрашивай статус деплоя до success или failure. При ошибке — вытащи логи и доложи.\n5. Сообщи финальный URL приложения.\n